export const DROPZONE_NAMES = {
  navigation: 'navigation-dropzone',
  layout: 'layout-dropzone',
};

export const DROPZONE_SIZE = {
  default: 60,
  layout: 250,
};

export const DEFAULT_PAGE_TITLE = 'Dashboard' as const;
export const DEFAULT_EDIT_MODE: boolean = false as const;

export const DEFAULT_DROPZONE_NAME = 'default-zone';

export const HEADER_HEIGHT = 66;
export const FLOATING_PANEL_MARGIN = 20;
export const SIDEBAR_PANEL_WIDTH = 460;
